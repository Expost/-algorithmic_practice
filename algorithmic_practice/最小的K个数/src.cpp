#include <stdio.h>
#include <vector>

using namespace std;

// 最基本思路，升序排序后，取前K个值
vector<int> GetLeastNumbers_Solution(vector<int> input, int k) 
{
    vector<int> v;
    return v;
}

int main()
{

    getchar();
    return 0;
}